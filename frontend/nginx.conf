# frontend/nginx.conf
server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This line ensures React/Vue handles 404s properly
        try_files $uri $uri/ /index.html; 
    }
}